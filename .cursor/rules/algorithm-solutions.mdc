---
description: 刷题站题解写作——窗口/DP 对齐左程云，链表/树求简洁易懂
globs: solutions/**/*.md
alwaysApply: false
---

# 题解写作规范

生成或改写 `solutions/**/*.md` 时，遵循 Agent Skill `algorithm-tutorial`，并按题型分流：

1. **滑动窗口 / 双指针 / 贪心 / 二分 / DP**：对齐 `/Users/zy/ai_learn/algorithm-journey` 课上模板（`l/r`、`cnts`/`debt`/`collect` 等）。
2. **链表 / 树 / 图结构题**：不必照搬课上竞赛写法（全局变量、极致 O(1) 手工迭代）；主解写**简洁易懂**的默写版，繁琐优化可作可选附录。
3. **落盘**：`solutions/{bankId}/{slug}.md`，题库 JSON 里 `hasSolution: true`。
4. **图表**：与现有 `solutions/base/*.md` 深色 Mermaid 风格一致。

反例（窗口题）：把课上 `l/r` 定长吐左改成另一套命名且与课上骨架不一致。  
反例（链表题）：为对齐竞赛代码而把清晰的递归归并换成难读的全局 `start/end` 迭代当主解。
